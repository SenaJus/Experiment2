#include<iostream>

using namespace std;
int main()
{
	int counter, num, sum;
	sum=0;
	cout<<"Enter number: ";
	cin>>num;
	for(counter = 1 ; counter <= num; counter ++)
	{
		sum+=counter;
	}
	cout <<"The sum is: "<< sum;
	
	return 0;
}
